import React from 'react'
import { Dimension } from '../../types'

const GameOptions = ({ width, height }: Dimension) => {
  return <div style={{ width, height }}>GameOptions</div>
}

export default GameOptions
